import { Card } from 'antd';

export function AdminDashboard() {
  return (
    <Card title="Admin Dashboard">
      <p>Mock stats sẽ làm ở F7. F0 chỉ cần route chạy được.</p>
    </Card>
  );
}
export function AdminPendingCars() {
  return <Card title="Duyệt tin">Sẽ làm ở F7.</Card>;
}
export function AdminUsers() {
  return <Card title="Quản lý User">Sẽ làm ở F7.</Card>;
}
